import VueTsTemplate from '../../../app/TsConverter/VueTsTemplate';
describe('TsConverter.VueTsTemplate', () => {
    it('render components list', () => {
        const renderData = {
            
        }
    })
});
