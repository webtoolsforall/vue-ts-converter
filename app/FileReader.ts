const fs = require('fs');
export default class FileReader {
    constructor(){
        console.log('bingo');
    }

    readFile(path:String): Promise {
        return new Promise((resolve, reject)=>{
            
        })
    }
}